#!/usr/bin/env node
import{existsSync as o}from"fs";import{dirname as r,join as a}from"path";import{fileURLToPath as i}from"url";import{spawnSync as s}from"child_process";var c=i(import.meta.url),u=r(c),l=a(u,"..",".tokenuse","bin","tokenuse"),t=process.env.TOKENUSE_NPM_BINARY_PATH||l;function e(n){console.warn(`[tokenuse] ${n}`)}function p(){if(!o(t)){e("native binary not found; skipping tracker cleanup");return}console.log("[tokenuse] stopping TokenUse background tracker before package removal...");let n=s(t,["uninstall","--keep-data"],{stdio:"inherit",env:process.env});if(n.error){e(`could not run tracker cleanup (${n.error.message}); continuing package removal`);return}n.status!==0&&e(`tracker cleanup exited with status ${n.status}; continuing package removal`)}p();
